{
  "name": "althea-pay",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "stripe": "^12.0.0",
    "pg": "^8.0.0"
  }
}
