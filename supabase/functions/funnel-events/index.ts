import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const URL=Deno.env.get("SUPABASE_URL")??""; const KEY=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??""; const SECRET=Deno.env.get("ALTHEA_INTERNAL_SECRET")??"";
const db=createClient(URL,KEY,{auth:{persistSession:false,autoRefreshToken:false}});
const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"apikey, authorization, content-type, x-althea-api-key, x-api-key, x-funnel-event-token, x-internal-secret, x-request-id","Access-Control-Allow-Methods":"POST,OPTIONS"};
const out=(x:unknown,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{...CORS,"Content-Type":"application/json"}});
const allowed=(x:string)=>["page_view","quiz_started","quiz_answered","lead_created","chat_started","chat_message","checkout_started","purchase","upsell","refund","chargeback","checkout_abandoned"].includes(x);
const sha256=async(v:string)=>{const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,"0")).join("");};
const clean=(v:unknown,max=500)=>typeof v==="string"&&v.trim()?v.trim().slice(0,max):null;
const object=(v:unknown):Record<string,unknown>=>v&&typeof v==="object"&&!Array.isArray(v)?v as Record<string,unknown>:{};
const customerOf=(payload:Record<string,unknown>)=>object(payload.customer??payload.buyer);
const customerField=(payload:Record<string,unknown>,keys:string[])=>{const customer=customerOf(payload);for(const key of keys){const a=clean(payload[key]);if(a)return a;const b=clean(customer[key]);if(b)return b;}return null;};
async function apiKey(req:Request){const k=req.headers.get("x-althea-api-key")||req.headers.get("x-api-key")||req.headers.get("apikey");if(!k)return null;const {data}=await db.rpc("authenticate_althea_api_key",{p_key:k}).maybeSingle();return data??null;}
async function ingestionToken(req:Request){const raw=req.headers.get("x-funnel-event-token");if(!raw)return null;const hash=await sha256(raw);const {data,error}=await db.rpc("resolve_funnel_ingestion_token",{p_token_hash:hash}).maybeSingle();if(error||!data)return null;return data;}
async function findActiveConversation(uid:string,funnelId:string,buyerEmail:string){const q=await db.from("crm_conversations").select("id,public_token,status").eq("user_id",uid).eq("funnel_id",funnelId).eq("buyer_email",buyerEmail).in("status",["open","pending"]).order("updated_at",{ascending:false}).limit(1).maybeSingle();if(q.error)throw q.error;return q.data;}
async function ensureConversation(uid:string,funnelId:string,payload:Record<string,unknown>,eventId:string|null){const buyerName=customerField(payload,["name","nome","full_name","buyer_name","customer_name"]);const buyerEmail=customerField(payload,["email","buyer_email","customer_email"]);const productId=clean(payload.product_id??payload.productId);const productName=clean(payload.product_name??payload.productName??payload.product);if(buyerEmail){const existing=await findActiveConversation(uid,funnelId,buyerEmail);if(existing)return{id:String(existing.id),token:String(existing.public_token??"")};}const token=Array.from(crypto.getRandomValues(new Uint8Array(32))).map(x=>x.toString(16).padStart(2,"0")).join("");const metadata={source:"funnel",customer:customerOf(payload),product_name:productName,...(eventId?{created_from_event_id:eventId}:{})};const created=await db.from("crm_conversations").insert({user_id:uid,funnel_id:funnelId,product_id:productId,buyer_name:buyerName,buyer_email:buyerEmail,status:"open",metadata,public_token:token}).select("id,public_token").single();if(created.error||!created.data){if(created.error?.code==="23505"&&buyerEmail){const raced=await findActiveConversation(uid,funnelId,buyerEmail);if(raced)return{id:String(raced.id),token:String(raced.public_token??"")};}throw created.error??new Error("conversation_create_failed");}return{id:String(created.data.id),token:String(created.data.public_token)};}
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:CORS});if(req.method!=="POST")return out({error:"method_not_allowed"},405);if(!URL||!KEY)return out({error:"server_not_configured"},500);const contentLength=Number(req.headers.get("content-length")||0);if(Number.isFinite(contentLength)&&contentLength>256*1024)return out({error:"payload_too_large"},413);const internal=!!(SECRET&&req.headers.get("x-internal-secret")===SECRET);const token=internal?null:await ingestionToken(req);const key=internal||token?null:await apiKey(req);if(!internal&&!token&&!key)return out({error:"unauthorized"},401);const body=await req.json().catch(()=>null) as Record<string,unknown>|null;if(!body)return out({error:"invalid_json"},400);if(JSON.stringify(body).length>100000)return out({error:"payload_too_large"},413);const type=clean(body.event_type,80);const funnelId=clean(body.funnel_id,255);const externalId=clean(body.external_id,255);if(!type||!allowed(type))return out({error:"invalid_event_type"},400);if(!funnelId)return out({error:"funnel_id_required"},400);const uid=internal?clean(body.user_id,100):(token?.user_id??key?.user_id);if(!uid)return out({error:"user_id_required"},400);if(token&&token.funnel_id!==funnelId)return out({error:"token_funnel_mismatch"},403);const {data:funnel,error:fe}=await db.from("funnels").select("id").eq("id",funnelId).eq("user_id",uid).maybeSingle();if(fe)return out({error:"database_error"},500);if(!funnel)return out({error:"funnel_not_found"},404);if(token)await db.from("funnel_ingestion_tokens").update({last_used_at:new Date().toISOString()}).eq("id",token.token_id).eq("user_id",uid);if(key){const scopes=Array.isArray(key.scopes)?key.scopes.map(String):[];if(!(scopes.includes("*")||scopes.includes("events:write")))return out({error:"insufficient_scope"},403);const rl=await db.rpc("consume_althea_api_rate_limit",{p_api_key_id:key.api_key_id,p_limit:120});const st=Array.isArray(rl.data)?rl.data[0]:rl.data;if(rl.error)return out({error:"rate_limit_unavailable"},503);if(!st?.allowed)return out({error:"rate_limited",reset_at:st.reset_at},429);}if(externalId){const {data:dup}=await db.from("integration_events").select("id,event_type,status,created_at").eq("user_id",uid).eq("external_id",externalId).maybeSingle();if(dup)return out({accepted:true,duplicate:true,event_id:dup.id,status:dup.status},200);}const payload=Object.keys(object(body.payload)).length?object(body.payload):body;const {data:event,error}=await db.from("integration_events").insert({user_id:uid,funnel_id:funnelId,event_type:type,external_id:externalId,status:"pending",payload,occurred_at:typeof body.occurred_at==="string"?body.occurred_at:new Date().toISOString()}).select("id,event_type,status,external_id,occurred_at,created_at").single();if(error)return out({error:error.code==="23505"?"duplicate_event":"event_rejected"},error.code==="23505"?200:400);let conversationToken:string|null=null;if(type==="chat_started"||type==="chat_message"){try{let conversation:{id:string;token:string};const suppliedToken=clean(body.conversation_token??payload.conversation_token,128);if(suppliedToken){const found=await db.from("crm_conversations").select("id,public_token").eq("user_id",uid).eq("funnel_id",funnelId).eq("public_token",suppliedToken).maybeSingle();if(found.error)throw found.error;if(!found.data)return out({error:"conversation_not_found"},404);conversation={id:String(found.data.id),token:String(found.data.public_token)};}else{conversation=await ensureConversation(uid,funnelId,{...payload,customer:body.customer??payload.customer},event.id);}conversationToken=conversation.token;if(type==="chat_message"){const text=clean(body.message??payload.message??payload.body,4000);if(!text)return out({error:"message_required"},400);const inserted=await db.from("crm_messages").insert({conversation_id:conversation.id,user_id:uid,direction:"inbound",channel:"webchat",body:text,metadata:{event_id:event.id,funnel_id:funnelId},created_at:new Date().toISOString()});if(inserted.error)throw inserted.error;}}catch(e){await db.from("integration_events").update({status:"retry",error_message:e instanceof Error?e.message:"crm_sync_failed"}).eq("id",event.id);return out({accepted:true,event_id:event.id,status:"retry",error:"crm_sync_failed"},202);}}
const sessionKey=clean(body.session_key??payload.session_key,255);if(sessionKey){try{await db.rpc("project_attribution_event",{p_user_id:uid,p_funnel_id:funnelId,p_session_key:sessionKey,p_event_type:type,p_payload:payload});}catch{}}
const claimed=await db.rpc("claim_integration_event",{p_event_id:event.id});if(claimed.error||claimed.data!==true)return out({accepted:true,event_id:event.id,status:"pending",...(conversationToken?{conversation_token:conversationToken}:{})},202);let automation="not_configured";if(SECRET){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),3500);try{const r=await fetch(`${URL}/functions/v1/automation-engine-v2`,{method:"POST",signal:controller.signal,headers:{"content-type":"application/json","x-internal-secret":SECRET},body:JSON.stringify({user_id:uid,funnel_id:funnelId,event_id:event.id,event_type:type,transaction_id:body.transaction_id,checkout_id:body.checkout_id,sale_id:body.sale_id,external_id:externalId,payload})});automation=r.ok?"triggered":`failed_${r.status}`;}catch{automation="failed_timeout_or_network";}finally{clearTimeout(timer);}}const status=automation==="triggered"||automation==="not_configured"?"processed":"retry";await db.from("integration_events").update({status,processed_at:status==="processed"?new Date().toISOString():null,error_message:status==="retry"?automation:null,next_retry_at:status==="retry"?new Date(Date.now()+60000).toISOString():null}).eq("id",event.id);const crmEvent=await db.from("crm_webhook_events").insert({user_id:uid,idempotency_key:`integration:${event.id}`,transaction_id:clean(body.transaction_id??payload.transaction_id,255),status:type,buyer_email:customerField(payload,["email","buyer_email","customer_email"]),buyer_name:customerField(payload,["name","nome","full_name","buyer_name","customer_name"]),payload:{...payload,funnel_id:funnelId,product_id:clean(body.product_id??payload.product_id??payload.productId),event_type:type,integration_event_id:event.id},received_at:typeof body.occurred_at==="string"?body.occurred_at:new Date().toISOString()});if(crmEvent.error&&crmEvent.error.code!=="23505")console.error("crm_mirror_failed",crmEvent.error.message);return out({accepted:true,event_id:event.id,status,automation,...(conversationToken?{conversation_token:conversationToken}: {})},202);});