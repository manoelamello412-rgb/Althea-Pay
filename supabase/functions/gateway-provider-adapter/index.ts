import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
type O=Record<string,unknown>;
const H={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"content-type,x-althea-internal-secret,x-gateway-id,x-althea-gateway-id,x-althea-idempotency-key","Access-Control-Allow-Methods":"POST,OPTIONS","Content-Type":"application/json","Cache-Control":"no-store"};
const obj=(v:unknown):v is O=>typeof v==="object"&&v!==null&&!Array.isArray(v);const str=(v:unknown)=>typeof v==="string"?v.trim():"";const json=(v:unknown,s=200)=>new Response(JSON.stringify(v),{status:s,headers:H});const nested=(v:unknown,k:string)=>obj(v)?str(v[k]):"";const err=(p:O,f:string)=>{const e=p.error;if(typeof e==="string")return e;if(obj(e)&&typeof e.message==="string")return e.message;if(Array.isArray(p.errors)&&obj(p.errors[0])&&typeof p.errors[0].description==="string")return p.errors[0].description;if(typeof p.message==="string")return p.message;return f};const norm=(provider:string,b:O)=>{const s=str(b.status??b.payment_status).toLowerCase();if(provider==="stripe")return ["succeeded","requires_capture"].includes(s)?"approved":["processing","requires_action","requires_confirmation"].includes(s)?"pending":["canceled","requires_payment_method"].includes(s)?"declined":"error";if(provider==="asaas")return ["received","confirmed","received_in_cash"].includes(s)?"approved":["pending","awaiting_risk_analysis","in_analysis"].includes(s)?"pending":["overdue","refunded","deleted","failed","chargeback"].includes(s)?"declined":"error";if(provider==="mercado_pago")return ["approved","authorized"].includes(s)?"approved":["in_process","pending","in_mediation"].includes(s)?"pending":["rejected","cancelled","cancelled_by_collector"].includes(s)?"declined":"error";return "error"};const parse=async(r:Response):Promise<O>=>{const x=await r.json().catch(()=>({}));return obj(x)?x:{}};
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:H});if(req.method!=="POST")return json({ok:false,error:"method_not_allowed"},405);const expected=Deno.env.get("ALTHEA_INTERNAL_SECRET")??"",supplied=req.headers.get("x-althea-internal-secret")??"";if(!expected||expected.length!==supplied.length)return json({ok:false,error:"forbidden"},403);let diff=0;for(let i=0;i<expected.length;i++)diff|=expected.charCodeAt(i)^supplied.charCodeAt(i);if(diff!==0)return json({ok:false,error:"forbidden"},403);let b:O;try{const x=await req.json();if(!obj(x))return json({ok:false,error:"invalid_json"},400);b=x}catch{return json({ok:false,error:"invalid_json"},400)}const provider=str(b.provider).toLowerCase(),operation=str(b.operation).toLowerCase(),gatewayId=str(b.gateway_id??req.headers.get("x-gateway-id")),environment=str(b.environment).toLowerCase()==="sandbox"?"sandbox":"production";if(!provider||!gatewayId)return json({ok:false,error:"provider_and_gateway_required"},422);const url=Deno.env.get("SUPABASE_URL"),service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!service)return json({ok:false,error:"server_configuration_error"},500);const db=createClient(url,service,{auth:{persistSession:false}});const reg=await db.from("gateway_provider_registry").select("provider_key,adapter_key,adapter_url,operational,is_active").eq("provider_key",provider).eq("is_active",true).maybeSingle();if(reg.error)return json({ok:false,error:"provider_registry_lookup_failed"},500);if(!reg.data)return json({ok:false,error:"provider_not_registered"},422);const gateway=await db.from("gateways").select("id,user_id,provider,environment,status").eq("id",gatewayId).maybeSingle();if(gateway.error||!gateway.data||String(gateway.data.provider).toLowerCase()!==provider||!["connected","degraded"].includes(String(gateway.data.status).toLowerCase()))return json({ok:false,error:"gateway_not_operational"},422);const cr=await db.rpc("resolve_gateway_credential_for_gateway",{p_gateway_id:gatewayId});if(cr.error||!obj(cr.data))return json({ok:false,error:"provider_credential_missing"},422);const c=cr.data;const credential=str(c.api_key??c.access_token??c.secret_key);const statusOps=["payment_status","retrieve_payment","retrieve","status"];if(!credential&&!statusOps.includes(operation))return json({ok:false,error:"provider_credential_missing"},422);if(!["stripe","asaas","mercado_pago"].includes(provider)){if(reg.data.operational!==true||!/^https:\/\//i.test(str(reg.data.adapter_url)))return json({ok:false,error:"provider_adapter_not_operational"},422);const r=await fetch(str(reg.data.adapter_url),{method:"POST",headers:{"Content-Type":"application/json","X-Althea-Internal-Secret":expected,"X-Althea-Gateway-Id":gatewayId,"X-Althea-Idempotency-Key":str(b.idempotency_key)},body:JSON.stringify({...b,credential:undefined})});return json(await parse(r),r.status)}try{if(statusOps.includes(operation)){const id=str(b.external_transaction_id??b.original_external_id??b.provider_transaction_id);if(!id)return json({ok:false,error:"external_transaction_id_required"},422);const api=provider==="stripe"?`https://api.stripe.com/v1/payment_intents/${encodeURIComponent(id)}`:provider==="asaas"?`${environment==="sandbox"?"https://api-sandbox.asaas.com/v3":"https://api.asaas.com/v3"}/payments/${encodeURIComponent(id)}`:`https://api.mercadopago.com/v1/payments/${encodeURIComponent(id)}`;const auth=provider==="stripe"?`Basic ${btoa(`${credential}:`)}`:`Bearer ${credential}`;const r=await fetch(api,{headers:{Authorization:auth}}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"status_lookup_failed"),failure_code:`http_${r.status}`,external_id:id},r.status);return json({ok:true,id:p.id??id,status:norm(provider,p),provider_status:str(p.status??p.payment_status),amount:provider==="stripe"?p.amount:Number(p.transaction_amount??p.value??0)*100,currency:String(p.currency??b.currency??"BRL").toUpperCase()})}const amount=Number(b.amount??0),currency=str(b.currency??"BRL").toUpperCase(),metadata=obj(b.metadata)?b.metadata:{},customer=obj(b.customer)?b.customer:{};if(!Number.isFinite(amount)||amount<=0||!/^[A-Z]{3}$/.test(currency))return json({ok:false,error:"invalid_money"},422);const token=str(b.payment_token),idem=str(b.idempotency_key??req.headers.get("x-althea-idempotency-key"));if(!idem)return json({ok:false,error:"idempotency_key_required"},422);const auth=provider==="stripe"?`Basic ${btoa(`${credential}:`)}`:`Bearer ${credential}`;
if(operation==="create_payment_link"){
 const linkType=str(b.link_type)||"payment_link",title=str(metadata.title??metadata.product_name??"ALTHEA PAY"),description=str(metadata.description??"Pagamento seguro via ALTHEA PAY");
 if(!["pix","card","payment_link"].includes(linkType))return json({ok:false,error:"invalid_link_type"},422);
 if(provider==="stripe"){
  const f=new URLSearchParams();f.set("line_items[0][price_data][product_data][name]",title.slice(0,250));f.set("line_items[0][price_data][product_data][description]",description.slice(0,500));f.set("line_items[0][price_data][currency]",currency.toLowerCase());f.set("line_items[0][price_data][unit_amount]",String(Math.round(amount)));f.set("line_items[0][quantity]","1");f.set("metadata[althea_payment_link_id]",str(metadata.payment_link_id));f.set("metadata[althea_idempotency_key]",idem);if(linkType==="card")f.set("payment_method_types[0]","card");if(linkType==="pix")f.set("payment_method_types[0]","pix");const r=await fetch("https://api.stripe.com/v1/payment_links",{method:"POST",headers:{Authorization:auth,"Content-Type":"application/x-www-form-urlencoded","Idempotency-Key":idem},body:f}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"stripe_payment_link_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,external_id:str(p.id),payment_url:str(p.url),status:"active",provider_status:p.active===true?"active":"inactive",amount,currency});
 }
 if(provider==="asaas"){
  const base=environment==="sandbox"?"https://api-sandbox.asaas.com/v3":"https://api.asaas.com/v3",billingType=linkType==="pix"?"PIX":linkType==="card"?"CREDIT_CARD":"UNDEFINED";const payload:O={name:title.slice(0,100),description:description.slice(0,500),value:amount/100,billingType,chargeType:"DETACHED",notificationEnabled:true,externalReference:str(metadata.payment_link_id)||idem};const r=await fetch(`${base}/paymentLinks`,{method:"POST",headers:{Authorization:`Bearer ${credential}`,"Content-Type":"application/json","X-Idempotency-Key":idem},body:JSON.stringify(payload)}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"asaas_payment_link_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,external_id:str(p.id),payment_url:str(p.url),status:"active",provider_status:"active",amount,currency});
 }
 if(provider==="mercado_pago"){
  const payload:O={items:[{title:title.slice(0,250),quantity:1,currency_id:currency,unit_price:amount/100}],external_reference:str(metadata.payment_link_id)||idem,metadata:{althea_payment_link_id:str(metadata.payment_link_id),althea_idempotency_key:idem}};const r=await fetch("https://api.mercadopago.com/checkout/preferences",{method:"POST",headers:{Authorization:`Bearer ${credential}`,"Content-Type":"application/json","X-Idempotency-Key":idem},body:JSON.stringify(payload)}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"mercado_pago_payment_link_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,external_id:str(p.id),payment_url:str(p.init_point??p.sandbox_init_point),status:"active",provider_status:"active",amount,currency});
 }
 return json({ok:false,error:"unsupported_provider"},422);
}
if(provider==="stripe"){if(!["create_payment","payment","refund"].includes(operation))return json({ok:false,error:"unsupported_operation"},422);if(operation==="refund"){const original=str(b.original_external_id);if(!original)return json({ok:false,error:"original_external_id_required"},422);const form=new URLSearchParams({payment_intent:original,amount:String(Math.round(amount))});const r=await fetch("https://api.stripe.com/v1/refunds",{method:"POST",headers:{Authorization:auth,"Content-Type":"application/x-www-form-urlencoded","Idempotency-Key":idem},body:form}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"stripe_refund_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,id:p.id,status:p.status==="succeeded"?"approved":"pending",amount:p.amount,currency:String(p.currency??currency).toUpperCase()})}let pm=str(metadata.payment_method_id);if(token&&!pm){if(token.startsWith("pm_"))pm=token;else if(token.startsWith("tok_")){const f=new URLSearchParams({type:"card","card[token]":token});const r=await fetch("https://api.stripe.com/v1/payment_methods",{method:"POST",headers:{Authorization:auth,"Content-Type":"application/x-www-form-urlencoded","Idempotency-Key":`${idem}:payment_method`},body:f}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"stripe_payment_method_failed"),failure_code:`http_${r.status}`},r.status);pm=str(p.id)}else return json({ok:false,error:"unsupported_stripe_payment_token"},422)}const f=new URLSearchParams({amount:String(Math.round(amount)),currency:currency.toLowerCase(),"metadata[althea_idempotency]":idem,confirm:pm?"true":"false"});if(pm)f.set("payment_method",pm);const r=await fetch("https://api.stripe.com/v1/payment_intents",{method:"POST",headers:{Authorization:auth,"Content-Type":"application/x-www-form-urlencoded","Idempotency-Key":idem},body:f}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"stripe_payment_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,id:p.id,status:norm(provider,p),provider_status:str(p.status),amount:p.amount,currency:String(p.currency??currency).toUpperCase()})}if(provider==="asaas"){const base=environment==="sandbox"?"https://api-sandbox.asaas.com/v3":"https://api.asaas.com/v3";if(operation==="refund"){const original=str(b.original_external_id);if(!original)return json({ok:false,error:"original_external_id_required"},422);const r=await fetch(`${base}/payments/${encodeURIComponent(original)}/refund`,{method:"POST",headers:{Authorization:`Bearer ${credential}`,"Content-Type":"application/json","X-Idempotency-Key":idem},body:JSON.stringify({value:amount/100})}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"asaas_refund_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,id:p.id??original,status:"approved",amount:Number(p.value??amount/100)*100,currency})}const pc=nested(customer,"provider_customer_id");if(!pc)return json({ok:false,error:"asaas_provider_customer_id_required"},422);const billing=str(metadata.billing_type)||"UNDEFINED";const payload:O={customer:pc,billingType:billing,value:amount/100,dueDate:str(metadata.due_date)||(new Date(Date.now()+86400000).toISOString().slice(0,10)),description:str(metadata.description)||"ALTHEA PAY charge"};if(token&&billing==="CREDIT_CARD")payload.creditCardToken=token;const r=await fetch(`${base}/payments`,{method:"POST",headers:{Authorization:`Bearer ${credential}`,"Content-Type":"application/json","X-Idempotency-Key":idem},body:JSON.stringify(payload)}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"asaas_payment_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,id:p.id,status:norm(provider,p),provider_status:str(p.status),amount:Number(p.value??amount/100)*100,currency})}if(operation==="refund"){const original=str(b.original_external_id);if(!original)return json({ok:false,error:"original_external_id_required"},422);const r=await fetch(`https://api.mercadopago.com/v1/payments/${encodeURIComponent(original)}/refunds`,{method:"POST",headers:{Authorization:`Bearer ${credential}`,"Content-Type":"application/json","X-Idempotency-Key":idem},body:JSON.stringify({amount:amount/100})}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"mercado_pago_refund_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,id:p.id,status:p.status==="approved"?"approved":"pending",amount:Number(p.amount??amount/100)*100,currency})}const pm=str(metadata.payment_method_id),email=nested(customer,"email");if(!pm||!token||!email)return json({ok:false,error:"mercado_pago_payment_method_token_and_customer_email_required"},422);const installments=Math.max(1,Math.min(36,Number(metadata.installments)||1));const r=await fetch("https://api.mercadopago.com/v1/payments",{method:"POST",headers:{Authorization:`Bearer ${credential}`,"Content-Type":"application/json","X-Idempotency-Key":idem},body:JSON.stringify({transaction_amount:amount/100,description:str(metadata.description)||"ALTHEA PAY charge",payment_method_id:pm,token,installments,payer:{email}})}),p=await parse(r);if(!r.ok)return json({ok:false,error:err(p,"mercado_pago_payment_failed"),failure_code:`http_${r.status}`},r.status);return json({ok:true,id:p.id,status:norm(provider,p),provider_status:str(p.status),amount:Number(p.transaction_amount??amount/100)*100,currency})}catch(e){return json({ok:false,error:"provider_adapter_error",detail:e instanceof Error?e.message:"unknown_error"},502)}});
