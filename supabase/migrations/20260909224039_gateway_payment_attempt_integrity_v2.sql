create unique index if not exists uq_gateway_attempt_order_per_sale on public.gateway_payment_attempts(user_id,sale_id,attempt_order); create index if not exists idx_gateway_payment_attempts_transaction_ref on public.gateway_payment_attempts(user_id,external_transaction_id,created_at desc); alter table public.gateway_payment_attempts add constraint gateway_payment_attempts_attempt_order_positive check (attempt_order > 0);