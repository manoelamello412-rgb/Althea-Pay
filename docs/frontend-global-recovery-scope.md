# Frontend Global Recovery Scope

The recovery pass prioritizes the observed symptom: the product frontend is visually stretched, sparse, and does not expose the breadth of existing platform behavior. Changes must be evidence-based and preserve canonical domain implementations.

Priority order: global shell and CSS cascade -> route composition -> data/action exposure -> responsive behavior -> component consistency -> domain-specific visual refinement -> validation.
