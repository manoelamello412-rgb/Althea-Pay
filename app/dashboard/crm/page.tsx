'use client'
import { FormEvent, useCallback, useEffect, useMemo, useRef, useState } from 'react'
import { Check, CheckCheck, ChevronLeft, Inbox, MessageCircle, RefreshCw, Search, Send, UserRound } from 'lucide-react'
import { createSupabaseBrowserClient } from '@/lib/supabase/client'
import { Button } from '@/components/ui/button'

type Json=Record<string,unknown>
type Conversation={id:string;user_id:string;funnel_id:string|null;product_id:string|null;transaction_id:string|null;buyer_name:string|null;buyer_email:string|null;status:string;assigned_to:string|null;metadata:Json;public_token:string|null;last_message_at:string|null;unread_count:number;created_at:string;updated_at:string;priority?:string;primary_channel?:string;customer_id?:string|null;customer_whatsapp?:string|null;checkout_status?:string;last_message_direction?:string|null}
type Message={id:string;conversation_id:string;user_id:string;direction:'inbound'|'outbound';channel:string;body:string;metadata:Json;created_at:string;client_message_id?:string|null;external_message_id?:string|null}
type Event={id:string;transaction_id:string|null;status:string;error_reason:string|null;buyer_email:string|null;buyer_name:string|null;payload:Json;received_at:string}
type Funnel={id:string;nome:string}
type Product={id:string;data:Json|null}
type Agent={id:string;name:string;status:string}
type Team={id:string;name:string;active:boolean}
type Cursor={updated_at:string;id:string}
type MessageCursor={created_at:string;id:string}
type Customer360=Json & {profile?:Json;aggregates?:Json;scores?:Json;conversations?:unknown[];messages?:unknown[];notes?:unknown[];tags?:unknown[];sales?:unknown[];checkouts?:unknown[];payment_attempts?:unknown[];events?:unknown[]}
type RealtimeState='connecting'|'connected'|'reconnecting'|'disconnected'|'synchronized'
const text=(v:unknown)=>typeof v==='string'&&v.trim()?v.trim():typeof v==='number'||typeof v==='boolean'?String(v):null
const obj=(v:unknown):Json=>v&&typeof v==='object'&&!Array.isArray(v)?v as Json:{}
const pick=(p:Json|null|undefined,keys:string[])=>{const r=obj(p);for(const k of keys){const v=text(r[k]);if(v)return v}for(const parent of ['customer','buyer']){const n=obj(r[parent]);for(const k of keys){const v=text(n[k]);if(v)return v}}return null}
const initials=(v:string)=>v.split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]?.toUpperCase()??'').join('')||'?'
const time=(v:string|null|undefined)=>{if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}

export default function CRMPage(){
 const supabase=useMemo(()=>createSupabaseBrowserClient(),[])
 const [uid,setUid]=useState(''),[conversations,setConversations]=useState<Conversation[]>([]),[messages,setMessages]=useState<Message[]>([]),[events,setEvents]=useState<Event[]>([]),[funnels,setFunnels]=useState<Funnel[]>([]),[products,setProducts]=useState<Product[]>([]),[agents,setAgents]=useState<Agent[]>([]),[teams,setTeams]=useState<Team[]>([]),[selectedId,setSelectedId]=useState<string|null>(null),[query,setQuery]=useState(''),[filter,setFilter]=useState<'all'|'unread'|'open'|'pending'|'closed'>('all'),[draft,setDraft]=useState(''),[loading,setLoading]=useState(true),[loadingMore,setLoadingMore]=useState(false),[loadingMessages,setLoadingMessages]=useState(false),[loadingMoreMessages,setLoadingMoreMessages]=useState(false),[sending,setSending]=useState(false),[rt,setRt]=useState<RealtimeState>('connecting'),[error,setError]=useState(''),[loading360,setLoading360]=useState(false),[assigning,setAssigning]=useState(false),[customer360,setCustomer360]=useState<Customer360|null>(null),[conversationCursor,setConversationCursor]=useState<Cursor|null>(null),[hasMoreConversations,setHasMoreConversations]=useState(false),[messageCursor,setMessageCursor]=useState<MessageCursor|null>(null),[hasMoreMessages,setHasMoreMessages]=useState(false),[conversationTotal,setConversationTotal]=useState(0)
 const selectedIdRef=useRef<string|null>(null);useEffect(()=>{selectedIdRef.current=selectedId},[selectedId])
 const loadConversations=useCallback(async(reset=true,silent=false)=>{if(!silent)setLoading(reset);const r=await supabase.rpc('crm_multicrm_conversations_page',{p_limit:50,p_cursor_updated_at:reset?null:conversationCursor?.updated_at??null,p_cursor_id:reset?null:conversationCursor?.id??null,p_query:query.trim()||null,p_filter:filter,p_agent_id:null,p_team_id:null,p_priority:null});if(r.error){setError(r.error.message);if(reset)setLoading(false);return false}const data=obj(r.data),items=Array.isArray(data.items)?data.items as Conversation[]:[],next=obj(data.next_cursor),cursor=next.updated_at&&next.id?{updated_at:String(next.updated_at),id:String(next.id)}:null;setConversations(xs=>reset?items:[...xs,...items.filter(n=>!xs.some(x=>x.id===n.id))]);setConversationCursor(cursor);setHasMoreConversations(Boolean(data.has_more));setConversationTotal(Number(data.total_count??items.length));setLoading(false);return true},[conversationCursor?.id,conversationCursor?.updated_at,filter,query,supabase])
 const loadMessages=useCallback(async(conversationId:string,reset=true)=>{const r=await supabase.rpc('crm_multicrm_messages_page',{p_conversation_id:conversationId,p_limit:100,p_cursor_created_at:reset?null:messageCursor?.created_at??null,p_cursor_id:reset?null:messageCursor?.id??null});if(r.error){setError(r.error.message);setLoadingMessages(false);return false}const data=obj(r.data),items=Array.isArray(data.items)?data.items as Message[]:[],next=obj(data.next_cursor),cursor=next.created_at&&next.id?{created_at:String(next.created_at),id:String(next.id)}:null;setMessages(xs=>{const merged=reset?items:[...xs,...items],map=new Map(merged.map(m=>[m.id,m]));return[...map.values()].sort((a,b)=>new Date(a.created_at).getTime()-new Date(b.created_at).getTime())});setMessageCursor(cursor);setHasMoreMessages(Boolean(data.has_more));setLoadingMessages(false);return true},[messageCursor?.created_at,messageCursor?.id,supabase])
 const loadEvents=useCallback(async(c:Conversation|null)=>{if(!c){setEvents([]);return}let q=supabase.from('crm_webhook_events').select('*').eq('user_id',uid).order('received_at',{ascending:false}).limit(50);if(c.transaction_id)q=q.eq('transaction_id',c.transaction_id);else if(c.buyer_email)q=q.ilike('buyer_email',c.buyer_email);else{setEvents([]);return}const r=await q;if(!r.error)setEvents((r.data??[]) as Event[])},[supabase,uid])
 const load=useCallback(async(silent=false)=>{const a=await supabase.auth.getUser();if(a.error||!a.data.user){setError('Sessão expirada.');setLoading(false);return false}const id=a.data.user.id;setUid(id);const[c,f,p,ag,tm]=await Promise.all([loadConversations(true,silent),supabase.from('funnels').select('id,nome').eq('user_id',id).is('deleted_at',null),supabase.from('products').select('id,data').eq('user_id',id),supabase.from('crm_agents').select('id,name,status').eq('user_id',id).order('name'),supabase.from('crm_teams').select('id,name,active').eq('user_id',id).eq('active',true).order('name')]);if(!c)return false;if(!f.error)setFunnels((f.data??[]) as Funnel[]);if(!p.error)setProducts((p.data??[]) as Product[]);if(!ag.error)setAgents((ag.data??[]) as Agent[]);if(!tm.error)setTeams((tm.data??[]) as Team[]);return true},[loadConversations,supabase])
 useEffect(()=>{void load()},[load])
 useEffect(()=>{const c=conversations.find(x=>x.id===selectedId)??null;if(!c){setMessages([]);setMessageCursor(null);setHasMoreMessages(false);setCustomer360(null);setEvents([]);return}setMessageCursor(null);setLoadingMessages(true);void loadMessages(c.id,true);void loadEvents(c);setLoading360(true);void supabase.rpc('crm_customer_360',{p_conversation_id:c.id}).then(r=>{if(r.error){setError(x=>x||r.error.message);setCustomer360(null)}else setCustomer360((r.data??null) as Customer360|null)}).finally(()=>setLoading360(false))},[selectedId])
 useEffect(()=>{if(!selectedId)return;const c=conversations.find(x=>x.id===selectedId);if(!c||c.unread_count<=0)return;let cancelled=false;void supabase.rpc('crm_operator_mark_read',{p_conversation_id:c.id}).then(r=>{if(cancelled)return;if(r.error)setError(x=>x||r.error.message);else setConversations(xs=>xs.map(x=>x.id===c.id?{...x,unread_count:0}:x))});return()=>{cancelled=true}},[conversations,selectedId,supabase])
 const reconcile=useCallback(async()=>{if(!uid)return false;setRt('reconnecting');const ok=await loadConversations(true,true);const id=selectedIdRef.current;if(id){await loadMessages(id,true);const c=conversations.find(x=>x.id===id);if(c)await loadEvents(c)}if(ok)setRt('synchronized');return ok},[conversations,loadConversations,loadEvents,loadMessages,uid])
 useEffect(()=>{if(!uid)return;let disposed=false;const channel=supabase.channel(`crm:${uid}`);const upsert=(row:Conversation)=>setConversations(xs=>{const i=xs.findIndex(x=>x.id===row.id);if(i<0)return[row,...xs];const copy=[...xs];copy[i]=row;return copy.sort((a,b)=>new Date(b.updated_at).getTime()-new Date(a.updated_at).getTime())});channel.on('postgres_changes',{event:'*',schema:'public',table:'crm_conversations',filter:`user_id=eq.${uid}`},p=>{if(disposed)return;if(p.eventType==='DELETE'){setConversations(xs=>xs.filter(x=>x.id!==String(p.old.id)));return}upsert(p.new as Conversation)});channel.on('postgres_changes',{event:'*',schema:'public',table:'crm_messages',filter:`user_id=eq.${uid}`},p=>{if(disposed)return;if(p.eventType==='DELETE'){setMessages(xs=>xs.filter(m=>m.id!==String(p.old.id)));return}const row=p.new as Message;setMessages(xs=>xs.some(m=>m.id===row.id)?xs:[...xs,row].sort((a,b)=>new Date(a.created_at).getTime()-new Date(b.created_at).getTime()))});channel.on('postgres_changes',{event:'*',schema:'public',table:'crm_webhook_events',filter:`user_id=eq.${uid}`},p=>{if(disposed)return;if(p.eventType==='DELETE'){setEvents(xs=>xs.filter(x=>x.id!==String(p.old.id)));return}const row=p.new as Event;setEvents(xs=>xs.some(x=>x.id===row.id)?xs:[row,...xs])});setRt('connecting');channel.subscribe(async status=>{if(disposed)return;if(status==='SUBSCRIBED')await reconcile();else if(status==='CHANNEL_ERROR'||status==='TIMED_OUT')setRt('reconnecting');else if(status==='CLOSED')setRt('disconnected')});const online=()=>{if(!disposed)void reconcile()};const offline=()=>setRt('disconnected');window.addEventListener('online',online);window.addEventListener('offline',offline);return()=>{disposed=true;window.removeEventListener('online',online);window.removeEventListener('offline',offline);void supabase.removeChannel(channel)}},[reconcile,supabase,uid])
 const funnelMap=useMemo(()=>new Map(funnels.map(x=>[x.id,x.nome])),[funnels]),productMap=useMemo(()=>new Map(products.map(x=>[x.id,pick(x.data,['name','nome','product_name'])||x.id])),[products]),agentMap=useMemo(()=>new Map(agents.map(x=>[x.id,x.name])),[agents]),latest=useMemo(()=>{const m=new Map<string,Message>();for(const x of messages){const old=m.get(x.conversation_id);if(!old||new Date(x.created_at).getTime()>new Date(old.created_at).getTime())m.set(x.conversation_id,x)}return m},[messages]),selected=useMemo(()=>conversations.find(c=>c.id===selectedId)??null,[conversations,selectedId]),selectedMessages=useMemo(()=>selected?[...messages.filter(m=>m.conversation_id===selected.id)].sort((a,b)=>new Date(a.created_at).getTime()-new Date(b.created_at).getTime()):[],[messages,selected]),selectedEvent=selected?events[0]??null:null,profile=useMemo(()=>{const p={...obj(selected?.metadata),...obj(selectedEvent?.payload),...obj(obj(customer360?.profile).conversation)},customer=obj(obj(customer360?.profile).customer);return{name:selected?.buyer_name||selectedEvent?.buyer_name||pick(customer,['name','nome','full_name'])||pick(p,['name','nome','full_name','customer_name'])||'Cliente',email:selected?.buyer_email||selectedEvent?.buyer_email||pick(customer,['email','buyer_email','customer_email'])||pick(p,['email','buyer_email','customer_email']),phone:selected?.customer_whatsapp||pick(customer,['whatsapp','phone','mobile'])||pick(p,['whatsapp','phone','mobile']),funnel:selected?.funnel_id?funnelMap.get(selected.funnel_id)??selected.funnel_id:pick(p,['funnel_name','funnel']),product:selected?.product_id?productMap.get(selected.product_id)??selected.product_id:pick(p,['product_name','product'])}},[customer360,funnelMap,productMap,selected,selectedEvent]),aggregates=obj(customer360?.aggregates),scores=obj(customer360?.scores)
 async function moreC(){if(!hasMoreConversations||loadingMore)return;setLoadingMore(true);await loadConversations(false,true);setLoadingMore(false)}
 async function moreM(){if(!selected||!hasMoreMessages||loadingMoreMessages)return;setLoadingMoreMessages(true);await loadMessages(selected.id,false);setLoadingMoreMessages(false)}
 async function assign(agentId:string|null,teamId:string|null,priority:string|null){if(!selected||assigning)return;setAssigning(true);try{const r=await supabase.rpc('crm_assign_conversation',{p_conversation_id:selected.id,p_agent_id:agentId,p_team_id:teamId,p_priority:priority});if(r.error)throw r.error;await loadConversations(true,true)}catch(x){setError(x instanceof Error?x.message:'Falha ao atribuir conversa.')}finally{setAssigning(false)}}
 async function send(e:FormEvent){e.preventDefault();if(!selected||!draft.trim()||sending||selected.status==='closed')return;setSending(true);try{const r=await supabase.rpc('crm_operator_send_message',{p_conversation_id:selected.id,p_body:draft.trim(),p_client_message_id:`${crypto.randomUUID()}-${Date.now()}`});if(r.error||!r.data)throw r.error??new Error('Mensagem não enviada.');const row=r.data as unknown as Message;setMessages(xs=>xs.some(m=>m.id===row.id)?xs:[...xs,row].sort((a,b)=>new Date(a.created_at).getTime()-new Date(b.created_at).getTime()));setDraft('')}catch(x){setError(x instanceof Error?x.message:'Falha ao enviar.')}finally{setSending(false)}}
 async function status(next:'open'|'pending'|'closed'){if(!selected)return;const r=await supabase.rpc('crm_operator_set_status',{p_conversation_id:selected.id,p_status:next});if(r.error){setError(r.error.message);return}await loadConversations(true,true)}
 const rtLabel={connecting:'CONECTANDO',connected:'REALTIME ATIVO',reconnecting:'RECONCILIANDO',disconnected:'DESCONECTADO',synchronized:'SINCRONIZADO'}[rt]
 const counts:Array<[string,unknown]>=[['Conversas',aggregates.conversations],['Vendas',aggregates.sales_count],['Checkouts',aggregates.checkout_count],['Pagamentos',aggregates.payment_attempts],['Notas',aggregates.notes_count]]
 return <div className="min-h-screen bg-[#070A09] text-slate-100"><header className="sticky top-0 z-20 border-b border-white/10 bg-[#070A09]/95 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-[1700px] items-center justify-between gap-4 px-4 lg:px-7"><div><div className="text-sm font-black tracking-[.2em]">ALTHEA PAY <span className="text-[#1DB854]">// MULTI-CRM</span></div><div className="mt-1 flex items-center gap-2 text-[9px] uppercase tracking-widest text-slate-600"><i className={`h-1.5 w-1.5 rounded-full ${rt==='connected'||rt==='synchronized'?'bg-[#1DB854]':rt==='disconnected'?'bg-rose-400':'bg-amber-400'}`}/>{rtLabel}</div></div><div className="hidden flex-1 md:block md:max-w-xl"><div className="mx-auto flex items-center gap-2 rounded-xl border border-white/10 bg-white/[.03] px-3 py-2"><Search size={15} className="text-slate-600"/><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void loadConversations(true)}} placeholder="Buscar cliente, mensagem, funil..." className="w-full bg-transparent text-xs outline-none placeholder:text-slate-600"/></div></div><Button type="button" variant="ghost" size="icon" onClick={()=>void reconcile()} aria-label="Sincronizar CRM" title="Sincronizar CRM"><RefreshCw size={16} className={loading?'animate-spin':''}/></Button></div></header><main className="mx-auto max-w-[1700px] px-3 py-5 lg:px-7"><div className="mb-4 flex flex-wrap items-end justify-between gap-3"><h1 className="mt-1 text-3xl font-black">Multi-CRM</h1><div className="flex gap-2 overflow-x-auto">{(['all','unread','open','pending','closed'] as const).map(x=><Button key={x} type="button" variant={filter===x?'default':'ghost'} size="sm" className="shrink-0 rounded-full" onClick={()=>{setFilter(x);setConversationCursor(null);setTimeout(()=>void loadConversations(true),0)}}>{x==='all'?'Todas':x==='unread'?'Não lidas':x==='open'?'Abertas':x==='pending'?'Pendentes':'Encerradas'} {x==='all'&&conversationTotal>0&&<span className="ml-1 opacity-60">{conversationTotal}</span>}</Button>)}</div></div>{error&&<div className="mb-4 rounded-xl border border-rose-500/20 bg-rose-500/10 p-3 text-xs text-rose-200">{error}</div>}<section className="grid min-h-[calc(100vh-180px)] overflow-hidden rounded-[24px] border border-white/10 bg-[#0B0F0D] lg:grid-cols-[340px_minmax(0,1fr)_320px]"><aside className={`${selected?'hidden lg:block':''} border-r border-white/10 bg-[#090C0B]`}><div className="border-b border-white/10 p-4"><div className="text-[10px] font-black uppercase tracking-widest text-slate-500">Conversas reais · {conversationTotal}</div><div className="mt-3 flex items-center gap-2 rounded-xl border border-white/10 px-3 py-2 md:hidden"><Search size={14} className="text-slate-600"/><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void loadConversations(true)}} placeholder="Buscar..." className="w-full bg-transparent text-xs outline-none"/></div></div><div className="overflow-y-auto">{loading?<div className="space-y-2 p-3">{[1,2,3,4].map(x=><div key={x} className="h-20 animate-pulse rounded-xl bg-white/[.03]"/>)}</div>:conversations.length===0?<div className="p-10 text-center text-sm text-slate-600"><Inbox className="mx-auto mb-3" size={28}/>Nenhuma conversa real.</div>:<>{conversations.map(c=>{const name=c.buyer_name||c.buyer_email||c.customer_whatsapp||'Cliente';return <Button key={c.id} type="button" variant="ghost" onClick={()=>setSelectedId(c.id)} className={`h-auto min-h-[96px] w-full justify-start rounded-none border-b border-white/[.06] p-4 text-left ${selected?.id===c.id?'bg-[#102218]':'hover:bg-white/[.025]'}`}><div className="flex w-full gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-white/[.05] text-xs font-black">{initials(name)}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><b className="truncate text-sm">{name}</b><span className="text-[9px] text-slate-600">{time(c.last_message_at||c.updated_at)}</span></div><div className="mt-1 truncate text-[9px] uppercase tracking-wider text-slate-600">{funnelMap.get(c.funnel_id??'')??'Sem funil'} · {c.primary_channel??'funnel_chat'}</div><p className="mt-1 truncate text-xs text-slate-500">{latest.get(c.id)?.body??'Sem mensagens nesta página'}</p></div>{c.unread_count>0&&<span className="flex h-5 min-w-5 items-center justify-center rounded-full bg-[#1DB854] px-1 text-[9px] font-black text-black">{c.unread_count}</span>}</div></Button>})}{hasMoreConversations&&<div className="p-3"><Button type="button" variant="ghost" className="w-full" disabled={loadingMore} onClick={()=>void moreC()}>{loadingMore?'Carregando...':'Carregar mais conversas'}</Button></div>}</>}</div></aside><div className={`${selected?'':'hidden lg:block'} min-w-0`}>{selected?<><div className="flex h-16 items-center justify-between border-b border-white/10 px-4"><div className="flex min-w-0 items-center gap-3"><Button type="button" variant="ghost" size="icon" onClick={()=>setSelectedId(null)} className="lg:hidden" aria-label="Voltar para conversas" title="Voltar para conversas"><ChevronLeft size={18}/></Button><div className="flex h-9 w-9 items-center justify-center rounded-full bg-[#12351F] text-xs font-black text-[#1DB854]">{initials(profile.name)}</div><div className="min-w-0"><b className="block truncate text-sm">{profile.name}</b><span className="block truncate text-[9px] text-slate-600">{profile.email??profile.phone??'Sem contato identificado'}</span></div></div><select value={selected.status} onChange={e=>void status(e.target.value as 'open'|'pending'|'closed')} className="rounded-xl border border-white/10 bg-[#090C0B] px-3 py-2 text-[10px] font-bold uppercase text-slate-300"><option value="open">Aberta</option><option value="pending">Pendente</option><option value="closed">Encerrada</option></select></div><div className="min-h-[500px] space-y-3 overflow-y-auto p-4 lg:p-7">{hasMoreMessages&&<Button type="button" variant="ghost" disabled={loadingMoreMessages} onClick={()=>void moreM()} className="mx-auto flex">{loadingMoreMessages?'Carregando...':'Carregar mensagens anteriores'}</Button>}{loadingMessages&&selectedMessages.length===0?<div className="flex min-h-80 items-center justify-center text-sm text-slate-600">Carregando mensagens...</div>:selectedMessages.length===0?<div className="flex min-h-80 items-center justify-center text-center text-sm text-slate-600"><div><MessageCircle className="mx-auto mb-3" size={30}/>Nenhuma mensagem registrada.</div></div>:selectedMessages.map(m=>{const md=obj(m.metadata),delivery=text(md.delivery_status)||text(md.status),provider=text(md.provider_message_id);return <div key={m.id} className={`flex ${m.direction==='outbound'?'justify-end':'justify-start'}`}><div className={`max-w-[82%] rounded-2xl px-4 py-3 ${m.direction==='outbound'?'bg-[#12351F]':'bg-white/[.045]'}`}><div className="whitespace-pre-wrap text-sm leading-6">{m.body}</div><div className="mt-1 flex justify-end gap-1 text-[9px] text-slate-600">{m.channel} · {time(m.created_at)} {m.direction==='outbound'&&(delivery==='failed'?<span>Falhou</span>:delivery==='sent'||provider||m.external_message_id?<CheckCheck size={11}/>:<Check size={11}/>)}</div></div></div>})}</div><form onSubmit={send} className="border-t border-white/10 p-3"><div className="flex items-end gap-2 rounded-2xl border border-white/10 p-2"><textarea value={draft} onChange={e=>setDraft(e.target.value)} disabled={sending||selected.status==='closed'} rows={2} maxLength={10000} placeholder={selected.status==='closed'?'Conversa encerrada':'Escreva uma mensagem...'} className="flex-1 resize-none bg-transparent p-2 text-sm outline-none placeholder:text-slate-600"/><Button type="submit" variant="default" size="icon" disabled={!draft.trim()||sending||selected.status==='closed'} aria-label="Enviar mensagem" title="Enviar mensagem"><Send size={16}/></Button></div></form></>:<div className="flex min-h-[600px] items-center justify-center text-sm text-slate-600"><div><MessageCircle className="mx-auto mb-3" size={36}/>Selecione uma conversa.</div></div>}</div><aside className={`${selected?'block':'hidden lg:block'} border-l border-white/10 bg-[#090C0B] p-5`}><div className="flex items-center gap-2 text-[9px] font-black uppercase tracking-widest text-[#1DB854]"><UserRound size={13}/> Customer 360</div>{selected&&<><div className="mt-5 rounded-2xl border border-white/10 p-4"><b className="text-lg">{profile.name}</b><div className="mt-2 space-y-1 text-xs text-slate-500"><div>{profile.email??'E-mail não identificado'}</div><div>{profile.phone??'WhatsApp não identificado'}</div></div></div><div className="mt-4 space-y-2 rounded-2xl border border-white/10 p-4 text-xs"><div className="flex justify-between"><span className="text-slate-600">Funil</span><b>{profile.funnel??'—'}</b></div><div className="flex justify-between"><span className="text-slate-600">Produto</span><b className="truncate pl-4">{profile.product??'—'}</b></div><div className="flex justify-between"><span className="text-slate-600">Checkout</span><b>{selected.checkout_status??'—'}</b></div><div className="flex justify-between"><span className="text-slate-600">Prioridade</span><select disabled={assigning} value={selected.priority??'normal'} onChange={e=>void assign(selected.assigned_to,null,e.target.value)} className="bg-transparent text-right text-xs"><option value="low">Baixa</option><option value="normal">Normal</option><option value="high">Alta</option></select></div><div className="flex justify-between"><span className="text-slate-600">Agente</span><select disabled={assigning} value={selected.assigned_to??''} onChange={e=>void assign(e.target.value||null,null,selected.priority??'normal')} className="max-w-[150px] truncate bg-transparent text-right text-xs"><option value="">Não atribuído</option>{agents.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></div><div className="flex justify-between"><span className="text-slate-600">Equipe</span><select disabled={assigning} value={String(obj(selected.metadata).team_id??'')} onChange={e=>void assign(selected.assigned_to,e.target.value||null,selected.priority??'normal')} className="max-w-[150px] truncate bg-transparent text-right text-xs"><option value="">Sem equipe</option>{teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div></div><div className="mt-4 rounded-2xl border border-white/10 p-4"><div className="text-[9px] uppercase tracking-widest text-slate-600">Customer 360 real</div>{loading360?<div className="mt-3 h-12 animate-pulse rounded-xl bg-white/[.03]"/>:<><div className="mt-3 grid grid-cols-2 gap-2">{counts.map(([label,value])=><div key={label} className="rounded-xl bg-white/[.025] p-2"><div className="text-[9px] text-slate-600">{label}</div><b>{text(value)??'0'}</b></div>)}</div><div className="mt-3 grid grid-cols-3 gap-2"><div className="rounded-xl bg-white/[.025] p-2"><div className="text-[8px] text-slate-600">Engajamento</div><b>{text(scores.engagement_score)??'0'}</b></div><div className="rounded-xl bg-white/[.025] p-2"><div className="text-[8px] text-slate-600">Conversão</div><b>{text(scores.conversion_score)??'0'}</b></div><div className="rounded-xl bg-white/[.025] p-2"><div className="text-[8px] text-slate-600">Recuperação</div><b>{text(scores.recovery_score)??'0'}</b></div></div></>}</div>{selectedEvent&&<div className="mt-4 rounded-2xl border border-white/10 p-4"><div className="text-[9px] uppercase tracking-widest text-slate-600">Último evento</div><b className="mt-2 block uppercase">{selectedEvent.status}</b>{selectedEvent.error_reason&&<p className="mt-1 text-xs text-rose-300">{selectedEvent.error_reason}</p>}</div>}</>}</aside></section></main></div>
}
