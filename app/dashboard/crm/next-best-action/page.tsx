'use client'

// Compatibility route kept for existing links. The canonical Next Best Action
// implementation lives in /dashboard/crm/actions to avoid maintaining two
// divergent frontends for the same backend contract.
export { default } from '../actions/page'
