'use client'

import { FormEvent, useEffect, useMemo, useState } from 'react'
import { useParams, useSearchParams } from 'next/navigation'
import { createSupabaseBrowserClient } from '@/lib/supabase/client'
import { FunnelWhiteLabelChat } from '@/components/funnel-white-label-chat'

type CheckoutContext={funnel:{id:string;name:string};offer:{id:string;name:string;price:number|string;currency:string;product_id:string;type:string};product:{id:string;name:string;description:string|null;product_type:string|null;billing_type:string;billing_interval:string|null;interval_count:number|null;unit_amount:number|string;currency:string}}
type PaymentAction={type:string;qr_code:string|null;copy_paste:string|null;url:string|null;expires_at:string|null;payment_method:string}
type PaymentResult={status:string;transactionId:string;checkoutSessionId:string;action:PaymentAction|null}
const terminalStatuses=new Set(['approved','failed','refunded','chargeback'])
function money(v:number|string,c:string){const n=Number(v);return Number.isFinite(n)?new Intl.NumberFormat('pt-BR',{style:'currency',currency:c||'BRL'}).format(n):'—'}

export default function PublicCheckoutPage(){
 const params=useParams<{funnelId:string}>(),searchParams=useSearchParams(),supabase=useMemo(()=>createSupabaseBrowserClient(),[])
 const funnelId=typeof params.funnelId==='string'?params.funnelId:'',offerId=searchParams.get('offer')
 const [context,setContext]=useState<CheckoutContext|null>(null),[name,setName]=useState(''),[email,setEmail]=useState(''),[phone,setPhone]=useState(''),[paymentMethod,setPaymentMethod]=useState('pix'),[loading,setLoading]=useState(true),[submitting,setSubmitting]=useState(false),[result,setResult]=useState<PaymentResult|null>(null),[error,setError]=useState(''),[chatEnabled,setChatEnabled]=useState(false),[idempotencyKey]=useState(()=>`checkout-${crypto.randomUUID()}`)
 useEffect(()=>{let active=true;async function load(){setLoading(true);setError('');setChatEnabled(false);if(!funnelId){setError('Checkout inválido.');setLoading(false);return}const [{data,error:e},chatResponse]=await Promise.all([supabase.rpc('get_public_checkout_context',{p_funnel_id:funnelId,p_offer_id:offerId||null}),fetch(`/api/funnel/chat?funnel_id=${encodeURIComponent(funnelId)}`,{cache:'no-store'}).catch(()=>null)]);if(!active)return;if(e)setError('Este checkout não está disponível no momento.');else setContext(data as CheckoutContext);if(chatResponse?.ok){const chatData=await chatResponse.json().catch(()=>null);if(active)setChatEnabled(chatData?.enabled===true)}setLoading(false)}void load();return()=>{active=false}},[funnelId,offerId,supabase])
 useEffect(()=>{if(!result?.transactionId||!result.checkoutSessionId||terminalStatuses.has(result.status))return;let active=true,timer:ReturnType<typeof setTimeout>|null=null;async function refresh(){if(!active)return;const r=await fetch('/api/payments/action',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({transaction_id:result!.transactionId,checkout_session_id:result!.checkoutSessionId,idempotency_key:idempotencyKey}),cache:'no-store'}).catch(()=>null);const d=await r?.json().catch(()=>null);if(!active)return;const status=typeof d?.status==='string'?d.status.toLowerCase():result!.status;setResult(current=>current?{...current,status,action:d?.ok&&d.action?d.action:current.action}:current);if(!terminalStatuses.has(status))timer=setTimeout(refresh,2500)}void refresh();return()=>{active=false;if(timer)clearTimeout(timer)}},[idempotencyKey,result?.transactionId,result?.checkoutSessionId,result?.status])
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setResult(null);if(!context)return;const n=name.trim(),em=email.trim().toLowerCase(),ph=phone.trim();if(n.length<2||n.length>160){setError('Informe seu nome completo.');return}if(!/^\S+@\S+\.\S+$/.test(em)||em.length>320){setError('Informe um e-mail válido.');return}if(ph.length>40){setError('Telefone inválido.');return}if(!['pix','card','boleto','wallet','bank_transfer','other'].includes(paymentMethod)){setError('Forma de pagamento inválida.');return}setSubmitting(true);try{const attribution=Object.fromEntries(['utm_source','utm_medium','utm_campaign','utm_content','utm_term','click_id'].flatMap(k=>{const v=searchParams.get(k);return v?[[k,v]]:[]}));const {data,e}=await supabase.rpc('create_public_checkout_session',{p_funnel_id:context.funnel.id,p_offer_id:context.offer.id,p_customer:{name:n,email:em,phone:ph},p_attribution:attribution,p_metadata:{source:'public_checkout'},p_idempotency_key:idempotencyKey});if(e||!data?.ok||!data?.session?.id)throw new Error('CHECKOUT_CREATE_FAILED');const pr=await fetch('/api/payments/create',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({checkout_session_id:data.session.id,payment_method:paymentMethod,idempotency_key:idempotencyKey})}),pd=await pr.json().catch(()=>null);if(!pr.ok&&!pd?.transaction?.id)throw new Error(pd?.code||'PAYMENT_CREATE_FAILED');if(!pd?.transaction?.id)throw new Error('PAYMENT_CREATE_FAILED');const status=String(pd.transaction.status||'').toLowerCase();setResult({status,transactionId:pd.transaction.id,checkoutSessionId:data.session.id,action:pd.action||null})}catch(c){console.error('[ALTHEA-CHECKOUT-PAYMENT]',c);setError('Não foi possível concluir a tentativa de pagamento. Tente novamente.')}finally{setSubmitting(false)}}
 if(loading)return <main className="min-h-screen bg-[#070b09] px-5 py-10 text-white"><div className="mx-auto max-w-xl animate-pulse rounded-3xl border border-white/10 bg-white/[0.03] p-8"><div className="h-7 w-2/3 rounded bg-white/10"/><div className="mt-4 h-4 w-full rounded bg-white/10"/><div className="mt-8 h-12 w-full rounded bg-white/10"/></div></main>
 if(error&&!context)return <main className="min-h-screen bg-[#070b09] px-5 py-10 text-white"><div className="mx-auto max-w-xl rounded-3xl border border-white/10 bg-white/[0.03] p-8 text-center"><h1 className="text-xl font-semibold">Checkout indisponível</h1><p className="mt-2 text-sm text-white/60">{error}</p></div></main>
 if(!context)return null
 const a=result?.action
 return <main className="min-h-screen bg-[#070b09] px-4 py-8 text-white sm:px-6 sm:py-12"><div className="mx-auto grid max-w-5xl gap-6 lg:grid-cols-[1fr_420px]"><section className="rounded-3xl border border-white/10 bg-white/[0.03] p-6 sm:p-8"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-white/45">{context.funnel.name}</p><h1 className="mt-3 text-2xl font-semibold tracking-tight sm:text-3xl">{context.product.name}</h1>{context.product.description&&<p className="mt-3 text-sm leading-6 text-white/60">{context.product.description}</p>}<div className="mt-8 rounded-2xl border border-white/10 bg-black/10 p-5"><p className="text-xs uppercase tracking-[0.18em] text-white/40">Oferta</p><div className="mt-2 flex items-end justify-between gap-4"><h2 className="font-medium">{context.offer.name}</h2><strong className="text-xl">{money(context.offer.price,context.offer.currency)}</strong></div></div></section><section className="rounded-3xl border border-white/10 bg-white/[0.045] p-6 shadow-2xl sm:p-7">{result?<div className="flex min-h-[320px] flex-col items-center justify-center text-center"><div className="flex h-14 w-14 items-center justify-center rounded-full border border-white/10 bg-white/5 text-2xl">{result.status==='approved'?'✓':result.status==='failed'?'!':result.status==='refunded'?'↩':result.status==='chargeback'?'!':'…'}</div><h2 className="mt-5 text-xl font-semibold">{result.status==='approved'?'Pagamento aprovado':result.status==='failed'?'Pagamento não aprovado':result.status==='refunded'?'Pagamento estornado':result.status==='chargeback'?'Pagamento em contestação':'Pagamento em processamento'}</h2>{a?.type==='pix'&&(a.qr_code||a.copy_paste)&&<div className="mt-6 w-full rounded-2xl border border-white/10 bg-black/20 p-4 text-left"><p className="text-xs font-semibold uppercase tracking-wider text-white/50">PIX</p>{a.qr_code&&/^data:image\//i.test(a.qr_code)&&<img src={a.qr_code} alt="QR Code PIX" className="mx-auto mt-4 h-48 w-48 rounded-xl bg-white p-2"/>}{a.copy_paste&&<><p className="mt-4 text-xs text-white/45">Código copia e cola</p><div className="mt-2 break-all rounded-xl bg-black/30 p-3 text-[11px] text-white/70">{a.copy_paste}</div><button type="button" onClick={()=>void navigator.clipboard?.writeText(a.copy_paste||'')} className="mt-3 h-10 w-full rounded-xl bg-white text-xs font-semibold text-black">Copiar código PIX</button></>}</div>}{a?.type==='redirect'&&a.url&&<a href={a.url} target="_blank" rel="noreferrer" className="mt-6 inline-flex h-11 items-center justify-center rounded-xl bg-white px-5 text-sm font-semibold text-black">Continuar para pagamento</a>}<p className="mt-4 text-sm leading-6 text-white/55">{result.status==='approved'?'O gateway confirmou a transação.':result.status==='failed'?'A transação não foi aprovada. Para uma nova tentativa, reinicie o checkout para gerar uma nova sessão de pagamento.':a?'A instrução de pagamento foi recebida do gateway. O status continua sendo acompanhado automaticamente.':'A transação foi encaminhada ao gateway e aguarda confirmação. Esta tela acompanha o status automaticamente.'}</p>{result.status==='failed'&&<button type="button" onClick={()=>window.location.reload()} className="mt-5 h-11 rounded-xl border border-white/10 bg-white px-5 text-sm font-semibold text-black">Iniciar nova tentativa</button>}<p className="mt-4 break-all text-[11px] text-white/30">Transação: {result.transactionId}</p></div>:<form onSubmit={submit} className="space-y-4"><div><h2 className="text-lg font-semibold">Seus dados</h2><p className="mt-1 text-xs text-white/45">Preencha os dados para continuar.</p></div>{error&&<div role="alert" className="rounded-xl border border-rose-400/20 bg-rose-400/10 px-3 py-2 text-xs text-rose-200">{error}</div>}<label className="block text-sm text-white/70">Nome<input value={name} onChange={e=>setName(e.target.value)} autoComplete="name" required className="mt-1.5 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3 text-white outline-none"/></label><label className="block text-sm text-white/70">E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required className="mt-1.5 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3 text-white outline-none"/></label><label className="block text-sm text-white/70">Telefone<input value={phone} onChange={e=>setPhone(e.target.value)} autoComplete="tel" inputMode="tel" className="mt-1.5 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3 text-white outline-none"/></label><fieldset><legend className="text-sm text-white/70">Forma de pagamento</legend><div className="mt-2 grid grid-cols-2 gap-2">{[['pix','PIX'],['card','Cartão'],['boleto','Boleto'],['wallet','Carteira'],['bank_transfer','Transferência'],['other','Outra']].map(([v,l])=><button key={v} type="button" onClick={()=>setPaymentMethod(v)} className={`rounded-xl border px-3 py-2.5 text-xs ${paymentMethod===v?'border-emerald-400/30 bg-emerald-400/10 text-emerald-300':'border-white/10 text-white/50'}`}>{l}</button>)}</div></fieldset><button type="submit" disabled={submitting} className="mt-2 h-12 w-full rounded-xl bg-white px-4 text-sm font-semibold text-black disabled:opacity-50">{submitting?'Processando pagamento…':'Continuar para pagamento'}</button></form>}</section></div>{chatEnabled&&<FunnelWhiteLabelChat funnelId={context.funnel.id} productName={context.product.name} brandName={`Suporte ${context.funnel.name}`} customer={{name:name.trim()||undefined,email:email.trim()||undefined,phone:phone.trim()||undefined}} />}</main>
}
